import {Component} from '@angular/core';
import {animate, style, transition, trigger} from '@angular/animations';export class AnimationsComponent {
  exp: any = false;
}@Component({
  selector: 'app-root',
 export class AppComponent {
}