#!/usr/bin/env bash   buildUrl="$1"}#   message=$(echo $message | sed 's/\r//g' | sed 's/\\/\\\\/g' | sed 's/"/\\"/g')  payl  echo  name="$1"  readonly safeBranchName=$(echo $CI_BRANCH | sed -e 's/\./_/g')   echo "$(payloadToJson)" > /tmp/current     addBuildUrl $CI_BUILD_URL    addMessage $CI_COMMIT  else    echo "Skipped uploading data for '$name', because this is a pull request."  fi    fi