/**
 * @license
 * Copyright Google LLC All Rights Reserved.
 *
 * Use of this source code is governed by an MIT-style license that can be
 * found in the LICENSE file at https://angular.io/license
 */    // report.    if (symbol.kind !== SymbolKind.Expression) {
      return [];    if (template        `the '?.' operator can be replaced with the '.' operator` :
        `the '?.' operator can be safely removed`;  advice}.`);
    return [diagnostic];
  }    ErrorCode.OPTIONAL_CHAIN_NOT_NULLABLE,
    ExtendedTemp    // Require `strictNullChecks` to be enabled.
    const strictNullChecks =    }    return new OptionalChainNotNullableCheck();
  },
};
