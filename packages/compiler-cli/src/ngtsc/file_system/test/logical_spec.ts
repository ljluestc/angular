/**
 * @license
 * Copyright Google LLC All Rights Reserved.
 *
 * Use of this source code is governed by an MIT-style license that can be
 * found in the LICENSE file at https://angular.io/license
 */        const fs = new LogicalFileSystem([_('/test'), _('/test/dist')], host);
             });        expect(rootFs.logicalPa      });        expect(fs.logicalPathOfFile(_('/Test/foo/Foo.ts')))
al('/foo/Foo' as LogicalProjectPath);
               e        if (host.useCaseSensiti      }); as LogicalProjectPath, '/bar' as LogicalProjectPath);
        expect(res).toEqual('./bar');
      }index' as      });
    });});