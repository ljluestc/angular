/**
 * @license
 * Copyright Google LLC All Rights Reserved.
 *
 * Use of this source code is governed by an MIT-style license that can be
 * found in the LICENSE file at https://angular.io/license
 */      fs.resolve(basePath, 'n  // Note that we intentionally pass the native `path`, without resolving it through the file
  // system, because the mock posix file system may break paths coming from a no    * file-system (via `FileSystem`). * @param fs the file-system where the directory is to be loaded.
 * @param directoryPath the p    }
  });