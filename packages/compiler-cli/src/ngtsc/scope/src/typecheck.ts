/**
 * @license
 * Copyright Google LLC All Rights Reserved.
 *
 * Use of this source code is governed by an MIT-style license that can be
 * found in the LICENSE file at https://angular.io/license
 */   * contains an error, then '    const matcher = new SelectorMatcher<DirectiveMeta[]>();
    const directives: Direct        if (extMeta === null) {
;is.hostDirectivesResolver.resolve(extMeta), extMeta]);
        directives.push(extMeta);yntaxKind[meta.ref.node.kind]} for pipe ${meta.ref.debugName}`)    }    const typeCheckScope: TypeCheckScop    const clazz = ref.node;
    i    }    const meta = flattenInheritedDirectiveMetadata(thi      return null;
    }
    this.flattenedDirectiveMet