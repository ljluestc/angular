/**
 * @license
 * Copyright Google LLC All Rights Reserved.
 *
 * Use of this source code is governed by an MIT-style license that can be
 * found in the LICENSE file at https://angular.io/license
 */    // don't affect the expo              // involve rewriting the `Reference`s to those types to have an alias expression if one is
        // required.path         } else {
necessary to rewrite the `Reference`s to add aliasad of a deep import directly to th      exported: {        isPoisoned: false,
      },
          if (this.aliasingHost === null || ref.node.getSourceFile() === maybeAliasFrom) {
      return dirOrPipe;    i    }    return {
      ...dirOrPipe,
      ref: ref.cloneWithAlias(alias),
    };}