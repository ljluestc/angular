/**
 * @license
 * Copyright Google LLC All Rights Reserved.
 *
 * Use of this source code is governed by an MIT-style license that can be
 * found in the LICENSE file at https://angular.io/license
 */
import {R3PartialDeclaration} from '@angular/compiler'; function call: It should have only a single object literal argument, but contained ${
.length}.`);    const emitScope = this.getEmitScope(ngImport, declarationScope);    const minVersion = metaOb  }   * that they can be inserted into the source code.  getConstantStatements(): {constantScope: TConstantScope, statements: TStatement[]}[] {
    const results: {constantScope: TConstantScope, statements: TStatement[]}[] = [];
    for (const [constantScope, emitScope] of this.emitScopes.entries()) {
     }  private getEmitScope(
      ngImport: TExpression, declarationScope: DeclarationScope<TCon    const constantScope = dec       }  }
}
