import {Component, NgModule} from '@angular/core';    getName: () => string,
    getLastName?: () => string,
    getSpecies?: () => () => () => () => () => string,
  } = {getName: () => 'Bilbo'};
}@NgModule({declarations: [MyApp]})
export class MyModule {
}