import {Component} from '@angular/core';  person?: {getName: (includeTitle: boolean|undefined) => string;};
  config: {
    get:
        (name: string) => {
 enabled: boolean
        } |
        undefined;
  }
}
