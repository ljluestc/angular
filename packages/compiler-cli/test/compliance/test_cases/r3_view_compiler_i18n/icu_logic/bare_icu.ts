import {Component, NgModule} from '@angular/core';    You have {count, select, 0 {no emails} 1 {one email} other {{{count}} emails}}.
  </div>
`
})
export class MyComponent {
  gender = 'female';
}@NgModule({declarations: [MyComponent]})
export class MyModule {
}