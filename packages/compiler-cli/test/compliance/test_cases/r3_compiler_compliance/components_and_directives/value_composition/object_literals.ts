import {Component, Input, NgModule} from '@angular/core';  @Input() config!: {[key: string]: any};
}@Component({
  selector: 'my-app',
 `
})
e}@NgModule({declarations: [ObjectComp, MyApp]})
export class MyModule {
}