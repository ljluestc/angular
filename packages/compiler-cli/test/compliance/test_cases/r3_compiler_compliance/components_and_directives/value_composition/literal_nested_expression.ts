import {Component, Input, NgModule} from '@angular/core';export class NestedComp {
  @Input() config!: {[key: string]: any};
}@Component({
  selector: 'my-app',
   </nested-comp>
`
}  name = 'slide';
  duration = 100;
}}
