/**
 * @license
 * Copyright Google LLC All Rights Reserved.
 *
 * Use of this source code is governed by an MIT-style license that can be
 * found in the LICENSE file at https://angular.io/license
 */
import {APP_INITIALIZER, ApplicationInitStatus} from '@angular/core/src/application_init';
import {EMPTY, Observable, Subscriber} from 'rxjs';    });    it('should handle a case                         } catch {    });
  });        });      TestBed.configureTestingModule(
{provid    });    it('should update the status once all async observable initializers are completed',
       async () => {initFnInvoked = true;
subscriatus.lizers(initFnInvoked = true;       });    it('should update the status if all async observable initializers are completed synchronously',
     co there waatus.done).toBe(true);
                    });      expect(status.done).toBe(false);
      try {        expect(status.done).toBe(false);
           }      t 'Please check that the `APP_INITIALIZER` token is configured as a ' +
 '`multi: true` provider    });
  });