# Note: This file is shipped to NPM. It cannot use the `defaults.bzl` macro.load("@build_bazel_rules_nodejs//:index.bzl", "nodejs_binary")package(default_visibility = ["//visibility:public"])filegroup(    name = "package_assets",    srcs = ["BUILD.bazel"],)nodejs_binary(    name = "rollup",    data = [m//@rollup/plugin-commonjs",  "@npllup/plugin-node-resolve",  "@npm//maging",  "@npm//rollup", //rollup-plugin-saps",  "@npm//typescript",    ],   _point = (  "@npm//:node_modules/rollup/dist/blup"    ),    # Disable the linker and rely on patched resolution which works better on Windows    # and is less prone to race conditions when targets build concurrently.    templated_args = ["--nobazel_run_linker"],)